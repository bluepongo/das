### query.GetByMySQLClusterID
GET http://{{baseURL}}/api/v1/query/cluster/{{mysql_cluster_id}}
Content-Type: application/json

{
    "token": "{{token}}",
    "start_time": "{{startTime}}",
    "end_time": "{{endTime}}",
    "limit": {{limit}},
    "offset": {{offset}}
}

### query.GetByMySQLServerID
GET http://{{baseURL}}/api/v1/query/server/{{mysql_server_id}}
Content-Type: application/json

{
    "token": "{{token}}",
    "start_time": "{{startTime}}",
    "end_time": "{{endTime}}",
    "limit": {{limit}},
    "offset": {{offset}}
}

### query.GetByHostInfo
GET http://{{baseURL}}/api/v1/query/host-info
Content-Type: application/json

{
    "token": "{{token}}",
    "host_ip": "{{mysql_host_ip}}",
    "port_num": {{mysql_port_num}},
    "start_time": "{{startTime}}",
    "end_time": "{{endTime}}",
    "limit": {{limit}},
    "offset": {{offset}}
}

### query.GetByDBID
GET http://{{baseURL}}/api/v1/query/db/{{db_id}}
Content-Type: application/json

{
    "token": "{{token}}",
    "mysql_server_id": {{mysql_server_id}},
    "start_time": "{{startTime}}",
    "end_time": "{{endTime}}",
    "limit": {{limit}},
    "offset": {{offset}}
}

### query.GetByMySQLSQLID
GET http://{{baseURL}}/api/v1/query/{{query_id}}
Content-Type: application/json

{
    "token": "{{token}}",
    "mysql_server_id": {{mysql_server_id}},
    "start_time": "{{startTime}}",
    "end_time": "{{endTime}}",
    "limit": {{limit}},
    "offset": {{offset}}
}

